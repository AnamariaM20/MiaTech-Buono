{/*import { useState } from "react";

const TextInput = () => {
    const [input, setInput] = useState("");

    const handleInput = (event) => {
        const {type, value} = event.target;
        console.log(value)
    } 
    return(
        <input type="text"  name = "input" value={""}  onInput={handleInput} onChangeText={(input) => {setInput(input)}}/>
    )
};
*/}
